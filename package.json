{
  "name": "marketing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "vue-cli-service lint",
    "deploy": "now --target production --public"
  },
  "dependencies": {
    "bootstrap": "^4.3.1"
  },
  "devDependencies": {}
}
